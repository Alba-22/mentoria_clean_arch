name: mentoria_clean_arch
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^4.0.6
  equatable: ^2.0.5
  flutter_secure_storage: ^6.0.0
  fpdart: ^0.3.0
  freezed_annotation: ^2.2.0
  json_annotation: ^4.7.0
  retrofit: ^3.3.1

dev_dependencies:
  build_runner:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  freezed:
  json_serializable: ^6.5.4
  mocktail:
  retrofit_generator: ^4.2.0
  value_listenable_test: ^0.0.1

flutter:
  uses-material-design: true
